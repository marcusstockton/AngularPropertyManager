import { PropertyDocument } from './property-document.model';

describe('PropertyDocument', () => {
  it('should create an instance', () => {
    expect(new PropertyDocument()).toBeTruthy();
  });
});
